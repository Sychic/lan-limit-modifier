# LAN Limit Modifier
Simple mod to allow changing the max player cap on LAN servers. 
Defaults to always being 1 over the current player count.
Set a hard cap by adding `-Dlanlimit.value=<your new limit>` to your JVM args.